# `templates/`

Templates HTML du dashboard Flask.

## Contenu

- `index.html` : interface principale (tableau de bord + moteur prédictif).

## Notes

- Les données affichées proviennent des endpoints Flask (`app.py`).
- Les métriques 2027 sont alimentées par les artefacts exportés du modèle.
